package com.example.pinit.ui.theme

import androidx.compose.ui.graphics.Color

// Primary colors
val PrimaryColor = Color(0xFF4E46E5) // Indigo primary
val SecondaryColor = Color(0xFF3B82F6) // Royal blue
val AccentColor = Color(0xFFEC4899) // Pink

// Background colors
val BgSurface = Color(0xFFF8FAFF) // Light bg surface (slightly bluer tint)
val BgCard = Color(0xFFFFFFFF) // White card background
val BgAccent = Color(0xFFF0F2FF) // Accented bg (light purple hue)
val BgSecondary = Color(0xFFF2F5FA) // Secondary bg (slightly bluer)

// Brand colors - vibrant and slightly more saturated
val BrandPrimary = Color(0xFF4F46E5) // Indigo primary
val BrandSecondary = Color(0xFF3B82F6) // Royal blue
val BrandAccent = Color(0xFFEC4899) // Pink
val BrandWarning = Color(0xFFF59E0B) // Amber
val BrandSuccess = Color(0xFF10B981) // Emerald

// Gradient colors
val GradientStart = Color(0xFF4F46E5) // Indigo start
val GradientMiddle = Color(0xFF5850EC) // Transition color
val GradientEnd = Color(0xFF6366F1) // Lighter indigo end

// Text colors with enhanced contrast
val TextPrimary = Color(0xFF0F172A) // Near black (darker)
val TextSecondary = Color(0xFF475569) // Slate 600
val TextLight = Color(0xFFFFFFFF) // White text
val TextMuted = Color(0xFF94A3B8) // Slate 400

// Social colors
val SocialDark = Color(0xFF142A50) // Dark blue
val SocialMedium = Color(0xFF285087) // Medium blue
val SocialPrimary = Color(0xFF4682D2) // Primary blue
val SocialAccent = Color(0xFF82C3EB) // Accent blue 
val SocialLight = Color(0xFFBEE1F5) // Light blue

// UI Elements
val Divider = Color(0xFFE2E8F0) // Slate 200
val CardShadow = Color(0xFF0F172A).copy(alpha = 0.08f) // Slate 900 8%
val CardStroke = Color(0xFFE2E8F0) // Slate 200 
val ColoredShadow = Color(0xFF4F46E5).copy(alpha = 0.15f) // Indigo shadow

// Additional UI elements
val CardHighlight = Color.White // Card highlight
val IconBg = Color(0xFFF0F5FF) // Icon background
val ActiveElement = Color(0xFF3B82F6) // Active element color

// Keep original theme colors for compatibility
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)

// Bottom Sheet Colors
val BottomSheetBg = Color(0xFFF8F9FA)

// Tab Colors
val TabSelectedBg = Color(0xFF2D3F50)
val TabUnselectedBg = Color(0xFFEEEEEE)
val TabSelectedText = Color.White
val TabUnselectedText = Color(0xFF666666)

// Accent Colors
val Accent = Color(0xFF3B82F6)
val AccentLight = Color(0xFFE1EFFE)